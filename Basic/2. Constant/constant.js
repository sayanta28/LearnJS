let interestRate = 0.3;
interestRate = 1;
console.log(interestRate);
// Here the variable intererstRate Can be Update. So the value can be change.

const interestRateRep = 0.3;
// interrestRateRep = 1; (Error)

console.log(interestRate);
//But here interestRateRep cannot be change. Const keyword gives the security.Once it is initialize it cannot be changed.
//That's why we should use const instead of let for giving the vasriable security.
